name: Deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.7.1
          use-dotnet: false
          include-templates: true

      - name: Import project
        run: godot --headless --editor --quit

      - name: Run rules tests
        run: godot --headless -s res://tests/run_rules_tests.gd

      - name: Run gameplay integration tests
        shell: bash
        run: |
          set -o pipefail
          godot --headless --quit-after 1200 -s res://tests/falling_feel_test_runner.gd 2>&1 | tee integration-test.log
          grep -q "Gameplay integration tests passed." integration-test.log

      - name: Run drop-only regression test
        shell: bash
        run: |
          set -o pipefail
          godot --headless -s res://tests/regression_drop_only_test.gd 2>&1 | tee drop-only-test.log
          grep -q "PASSED" drop-only-test.log

      - name: Run board-jammed tests
        shell: bash
        run: |
          set -o pipefail
          godot --headless -s res://tests/board_jammed_runner.gd 2>&1 | tee board-jammed-test.log
          grep -q "Board-jammed integration tests passed." board-jammed-test.log

      - name: Export Web build
        run: |
          mkdir -p build/web
          godot --headless --export-release Web build/web/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
